#include<iostream>
using namespace std;
int main()
{
    int n,row,col;
    for(row=0;row<n;row++;)
    {
        for(col=0;col<n+1;col++)
        {
            if((row==0 && col%3!=0) || (row==1 && col%3==0) || )
        }
    }
}